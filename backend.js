//creating an express server:

//importing the express package in the express variable. It gets every functionalities that the express module has exported.
//it is a function that is imported so, express(variable) is now a function.
const express = require("express");

//creating an instance of the express aplication.
//as everything is in the express() so we need to create a instance of the function which represents us our express application.
const app = express();